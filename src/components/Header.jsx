import React from 'react'

function Header({titulo}){


    //lo que esta dentro del return es lllo que se va a ver en pantalla 
    return(
    <h1 className="encabezado">{titulo}</h1>
    )
}
export default Header;